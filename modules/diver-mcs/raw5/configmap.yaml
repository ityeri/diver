apiVersion: v1
kind: ConfigMap
metadata:
  name: raw5-config
  namespace: diver-mcs
data:
  paper-global.yml: |
    proxies:
      velocity:
        enabled: true
        online-mode: false
        secret: ${CFG_VELOCITY_SECRET}
    chunk-saver:
      auto-save-interval: 1200
